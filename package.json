{
  "name": "SmartConnectAPIFaker",
  "version": "1.0.0",
  "description": "Fake backend API for SmartConnect",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT",
    "dev": "json-server --watch db.json --port 3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "0.17.4"
  },
  "engines": {
    "node": ">=14"
  }
}
